

const ObjecttoProps = (props) => {
    return(
        <>
            <h1>{props.person.name} {props.person.text} the exam with {props.person.mark} marks.</h1>
        </>
    )
}

export default ObjecttoProps;